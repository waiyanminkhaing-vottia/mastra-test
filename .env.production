# Production Environment Configuration
# Note: Sensitive values should be injected via deployment environment variables

# Logging configuration
LOG_LEVEL=info

# Cache configuration
AGENT_CONFIG_CACHE_TTL=60

# Database configuration (will be overridden by deployment)
DATABASE_URL=postgresql://prod_user:prod_password@prod-db:5432/mastra_test_prod
AGENT_DATABASE_URL=postgresql://prod_user:prod_password@prod-db:5432/mastra_agents_prod

# API Keys (will be overridden by deployment secrets)
OPENAI_API_KEY=your-prod-openai-api-key

# Zapier MCP Configuration
ZAPIER_MCP_URL=https://your-zapier-mcp-url.com